pipeline {
  agent {
        docker {
            label 'docker'
        }
    }
  stages {
    stage('build') {
      parallel {
        stage('build') {
          steps {
            echo 'building the code'
          }
        }
      }
    }
  }
}
