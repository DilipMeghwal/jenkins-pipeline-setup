pipeline {
  agent {
        docker {
            image 'jenkins/agent'
            label 'docker'
        }
    }
  stages {
    stage('build') {
      parallel {
        stage('build') {
          steps {
            echo 'building the code'
          }
        }
      }
    }
  }
}
