pipeline {
  agent "jenkins-agent"
  stages {
    stage('build') {
      parallel {
        stage('build') {
          steps {
            echo 'building the code'
          }
        }
      }
    }
  }
}
